git branch note//create branch
git checkout note //switch branch
git add .
git commit -m "xong note git"
git log --oneline --decorate //log branch
git push -u huyen note //up

1. git status
2. git add .
3. git commit -m "chú thích"
4. git push -u huyen note 